#!/bin/sh
# preinst script for perfsonar-archive

groupadd -r perfsonar 2> /dev/null || :
useradd -g perfsonar -r -s /sbin/nologin -c "perfSONAR User" -d /tmp perfsonar 2> /dev/null || :

#DEBHELPER#

exit 0
